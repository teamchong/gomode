//! Host imports — available for Zig code that needs runtime services.
//! Currently unused — all Zig functions are pure compute (SIMD, memory).
//! Will be needed when adding crypto, HTTP parsing, etc.
